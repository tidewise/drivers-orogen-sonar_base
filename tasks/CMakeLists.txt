# Generated from orogen/lib/orogen/templates/tasks/CMakeLists.txt

include(sonar_baseTaskLib)
ADD_LIBRARY(${SONAR_BASE_TASKLIB_NAME} SHARED
    ${SONAR_BASE_TASKLIB_SOURCES})
add_dependencies(${SONAR_BASE_TASKLIB_NAME}
    regen-typekit)


TARGET_LINK_LIBRARIES(${SONAR_BASE_TASKLIB_NAME}
    ${OrocosRTT_LIBRARIES}
    ${QT_LIBRARIES}
    ${SONAR_BASE_TASKLIB_DEPENDENT_LIBRARIES})
SET_TARGET_PROPERTIES(${SONAR_BASE_TASKLIB_NAME}
    PROPERTIES LINK_INTERFACE_LIBRARIES "${SONAR_BASE_TASKLIB_INTERFACE_LIBRARIES}")
SET_TARGET_PROPERTIES(${SONAR_BASE_TASKLIB_NAME}
    PROPERTIES INTERFACE_LINK_LIBRARIES "${SONAR_BASE_TASKLIB_INTERFACE_LIBRARIES}")


INSTALL(TARGETS ${SONAR_BASE_TASKLIB_NAME}
    RUNTIME DESTINATION bin
    LIBRARY DESTINATION lib/orocos)

INSTALL(FILES ${SONAR_BASE_TASKLIB_HEADERS}
    DESTINATION include/orocos/sonar_base)

